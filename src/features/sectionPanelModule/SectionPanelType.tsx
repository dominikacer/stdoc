export interface SectionPanelItemType {
    icon: string,
    title: string,
    subtitle: string,
}

export interface SectionPanelType {
    sectionTitle: string,
    items: SectionPanelItemType[],
}
